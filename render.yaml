services:
  - type: web
    name: media-tool
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: BACKEND_URL
        value: http://localhost:8100
    healthCheckPath: /
